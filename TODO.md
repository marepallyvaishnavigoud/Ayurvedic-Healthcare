# TODO

## Planned Fixes: Doctors/Meds/Treatments images + page behavior

- [x] Patch `frontend/src/pages/Medicines.jsx` for robust image mapping (normalize + safe fallbacks)
- [x] Patch `frontend/src/pages/Treatments.jsx` for robust image mapping (normalize + fallback to local images even if casing differs)
- [x] Patch `frontend/src/pages/Doctors.jsx` for defensive image src resolution + friendly empty state
- [ ] Run frontend build/tests to confirm no runtime errors


